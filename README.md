# ESGI_TP_Elasticsearch

```bash
# Start Kibana and ElasticSearch
docker-compose up -d
# Open the php docker container bash
docker-compose run --rm php bash
# Populate the ElasticSearch
php index.php 

```